# Project instructions

- Commit and push directly to `main`; do not create PRs unless the user asks.
- Submit Iris GPU jobs at batch priority.
- Use only the fixed validation split for architecture and checkpoint selection.
  Keep held-out evaluation separate from improvement decisions.
